import { Outlet } from 'react-router'

export default function RootLayout() {
  return (
    <div style={{ background: 'linear-gradient(135deg,rgb(234, 175, 102) 0%, #764ba2 100%)' }}>
      66666
      <Outlet />
    </div>
  )
}
